package business.entities.character;

/**
 * Esta clase sera la encargada de representar el presonaje de nuestro usuario
 */
public class Player extends Character{

    /**
     * Este metodo es el constructor de nuestro jugador
     * @param color el color que deseamos que tenga
     */
    public Player(String color) {super(color);}
}
